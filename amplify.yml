version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm
            - pnpm install --force
        build:
          commands:
            - pnpm build
      artifacts:
        baseDirectory: packages/client/.next
        files:
          - "**/*"
      cache:
        paths:
          - packages/client/node_modules/**/*
    appRoot: packages/client
